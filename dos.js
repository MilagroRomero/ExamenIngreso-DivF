function mostrar()
{
 let nombre;
 let situacionLaboral;
 let cantMaterias
 let sexo;
 let notaPromedio
 let edad;




{

nombre = prompt ("Ingrese su nombre")

		sexo = prompt("Ingrese su sexo: femenino, masculino o no binario");
		while(isNaN(tipo) == false || tipo != "femenino" && tipo != "masculino" && tipo != "no binario")
	{
		sexo = prompt("Error. Ingrese su sexo validio");
	}

while(isNaN(situacionLaboral) == false || situacionLaboral != "buscando" && situacionLaboral != "trabajando" && situacionLaboral != "solo estudiante")

cantMaterias = parseFloat(prompt("Ingrese la cantidad de materias (mayor a 0 y menor a 8)"));
	while(isNaN(cantMaterias) || cantMaterias < 0 || cantMaterias > 8){
		precio = parseFloat(prompt("Error. Ingrese la cantidad de materias (mayor a 0 y menor a 8)"));


		notaPromedio = parseFloat(prompt("Ingrese su nota promedio (entre 0 y 10)"));
	while(isNaN(notaPromedio) || notaPromedio < 0 || notaPromedio > 8){
		notaPromedio = parseFloat(prompt("Error. Ingrese su nota promedio (entre 0 y 10)"));


		nombre = prompt ("Ingrese su nombre")
}
